---
layout: base.njk
title: "Product Reviews & Buying Guides"
description: "Honest, in-depth product reviews to help you make informed buying decisions"
---

<section class="blog-listing">
  <div class="container">
    <header class="blog-header">
      <h1>Latest Product Reviews</h1>
      <p>Honest, detailed reviews to help you choose the right products</p>
    </header>

    <div class="posts-grid">
      {% for post in collections.posts %}
      <article class="post-card">
        <div class="post-card-content">
          <span class="post-card-category">{{ post.data.category }}</span>
          <h2 class="post-card-title">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h2>
          <p class="post-card-excerpt">{{ post.data.excerpt }}</p>
          
          {% if post.data.product %}
          <div class="post-card-rating">
            <span class="rating-stars-small">{{ post.data.product.rating }}</span>
            <span class="rating-text">{{ post.data.product.score }}/5</span>
          </div>
          {% endif %}

          <div class="post-card-meta">
            <span class="post-card-author">{{ post.data.author }}</span>
            <span class="post-card-date">{{ post.data.date | readableDate }}</span>
          </div>
          
          <a href="{{ post.url }}" class="post-card-link">Read Review →</a>
        </div>
      </article>
      {% endfor %}
    </div>
  </div>
</section>
