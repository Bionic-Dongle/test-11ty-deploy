<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="{{ siteContent.meta_description or 'Your site description' }}">
  <title>{{ siteContent.meta_title or 'My Site' }}</title>

  <!-- Styles -->
  <link rel="stylesheet" href="/assets/css/global.css" />

  <!-- Security Headers -->
  <meta http-equiv="Content-Security-Policy" content="default-src 'self' 'unsafe-inline' https:; img-src 'self' https: data:;" />
  <meta http-equiv="X-Content-Type-Options" content="nosniff" />
  <meta http-equiv="X-Frame-Options" content="DENY" />
</head>
<body>

  <div class="site-container">
    
    <!-- Header with Logo -->
      <div class="site-header">
        <a href="/">{{ siteContent.header.logo_text }}</a>
      </div>
    
    <!-- Navigation Bar -->
    <nav class="site-nav">
      <ul>
        {% for link in siteContent.header.nav_links %}
        <li><a href="{{ link.href }}">{{ link.text }}</a></li>
        {% endfor %}
      </ul>
    </nav>
    
    <!-- Hero (Optional) -->
    {% include "hero.njk" %}
    
    <!-- Two Column Layout -->
    <div class="content-wrapper">
      <main class="main-content">
        {% if siteContent.author and siteContent.author.image %}
        <div class="author-image">
          <img src="{{ siteContent.author.image }}" alt="{{ siteContent.author.name }}">
        </div>
        {% endif %}
        
        {{ content | safe }}
      </main>

      {% include "sidebar.njk" %}
    </div>
    
    <!-- Footer -->
    <footer class="site-footer">
      <p>{{ siteContent.footer.text }}</p>
    </footer>
    
  </div>

</body>
</html>
